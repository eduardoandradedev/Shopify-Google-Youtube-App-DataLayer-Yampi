<script>
(function(){
    var cookieName = "Lead_Email"; // Nome do cookie
    var cookieValue = "{{User Data | Email (DLV) - order.customer.email}}" || ""; // Valor do cookie
    var expirationTime = 2592000 * 1000; // Um mês em milissegundos

    var date = new Date();
    date.setTime(date.getTime() + expirationTime);
    var expires = "expires=" + date.toUTCString();

    // Obtém o domínio de nível superior, removendo subdomínios
    var domainParts = location.hostname.split('.');
    var domain = domainParts.length > 2 
        ? "." + domainParts.slice(-2).join('.')  // Considera apenas os dois últimos segmentos
        : "." + location.hostname.replace(/^www\./i, ""); // Remove 'www.' para cobrir subdomínios

    // Define o cookie
    document.cookie = cookieName + "=" + encodeURIComponent(cookieValue) + 
                      "; " + expires + 
                      "; path=/; domain=" + domain + 
                      "; Secure; SameSite=None;";
})();
</script>


<script>
(function(){
    var cookieName = "Lead_FirstName_GADs"; // Nome do cookie
    var cookieValue = "{{User Data | FirstName (DLV) - order.customer.first_name}}" || ""; // Valor do cookie
    var expirationTime = 2592000 * 1000; // Um mês em milissegundos

    var date = new Date();
    date.setTime(date.getTime() + expirationTime);
    var expires = "expires=" + date.toUTCString();

    // Obtém o domínio de nível superior, removendo subdomínios
    var domainParts = location.hostname.split('.');
    var domain = domainParts.length > 2 
        ? "." + domainParts.slice(-2).join('.')  // Considera apenas os dois últimos segmentos
        : "." + location.hostname.replace(/^www\./i, ""); // Remove 'www.' para cobrir subdomínios

    // Define o cookie
    document.cookie = cookieName + "=" + encodeURIComponent(cookieValue) + 
                      "; " + expires + 
                      "; path=/; domain=" + domain + 
                      "; Secure; SameSite=None;";
})();
</script>


<script>
(function(){
    var cookieName = "Lead_LastName_GADs"; // Nome do cookie
    var cookieValue = "{{User Data | LastName (DLV) - order.customer.last_name}}" || ""; // Valor do cookie
    var expirationTime = 2592000 * 1000; // Um mês em milissegundos

    var date = new Date();
    date.setTime(date.getTime() + expirationTime);
    var expires = "expires=" + date.toUTCString();

    // Obtém o domínio de nível superior, removendo subdomínios
    var domainParts = location.hostname.split('.');
    var domain = domainParts.length > 2 
        ? "." + domainParts.slice(-2).join('.')  // Considera apenas os dois últimos segmentos
        : "." + location.hostname.replace(/^www\./i, ""); // Remove 'www.' para cobrir subdomínios

    // Define o cookie
    document.cookie = cookieName + "=" + encodeURIComponent(cookieValue) + 
                      "; " + expires + 
                      "; path=/; domain=" + domain + 
                      "; Secure; SameSite=None;";
})();
</script>


<script>
(function(){
    var cookieName = "Lead_PhoneNumber_GADs"; // Nome do cookie
    var cookieValue = "{{User Data | Norm - PhoneNumber (GADs)}}" || ""; // Valor do cookie
    var expirationTime = 2592000 * 1000; // Um mês em milissegundos

    var date = new Date();
    date.setTime(date.getTime() + expirationTime);
    var expires = "expires=" + date.toUTCString();

    // Obtém o domínio de nível superior, removendo subdomínios
    var domainParts = location.hostname.split('.');
    var domain = domainParts.length > 2 
        ? "." + domainParts.slice(-2).join('.')  // Considera apenas os dois últimos segmentos
        : "." + location.hostname.replace(/^www\./i, ""); // Remove 'www.' para cobrir subdomínios

    // Define o cookie
    document.cookie = cookieName + "=" + encodeURIComponent(cookieValue) + 
                      "; " + expires + 
                      "; path=/; domain=" + domain + 
                      "; Secure; SameSite=None;";
})();
</script>


<script>
(function(){
    var cookieName = "Lead_City_GADs"; // Nome do cookie
    var cookieValue = "{{User Data | City (DLV) - order.address_info.city}}" || ""; // Valor do cookie
    var expirationTime = 2592000 * 1000; // Um mês em milissegundos

    var date = new Date();
    date.setTime(date.getTime() + expirationTime);
    var expires = "expires=" + date.toUTCString();

    // Obtém o domínio de nível superior, removendo subdomínios
    var domainParts = location.hostname.split('.');
    var domain = domainParts.length > 2 
        ? "." + domainParts.slice(-2).join('.')  // Considera apenas os dois últimos segmentos
        : "." + location.hostname.replace(/^www\./i, ""); // Remove 'www.' para cobrir subdomínios

    // Define o cookie
    document.cookie = cookieName + "=" + encodeURIComponent(cookieValue) + 
                      "; " + expires + 
                      "; path=/; domain=" + domain + 
                      "; Secure; SameSite=None;";
})();
</script>


<script>
(function(){
    var cookieName = "Lead_State_GADs"; // Nome do cookie
    var cookieValue = "{{User Data | Norm - State (GADs)}}" || ""; // Valor do cookie
    var expirationTime = 2592000 * 1000; // Um mês em milissegundos

    var date = new Date();
    date.setTime(date.getTime() + expirationTime);
    var expires = "expires=" + date.toUTCString();

    // Obtém o domínio de nível superior, removendo subdomínios
    var domainParts = location.hostname.split('.');
    var domain = domainParts.length > 2 
        ? "." + domainParts.slice(-2).join('.')  // Considera apenas os dois últimos segmentos
        : "." + location.hostname.replace(/^www\./i, ""); // Remove 'www.' para cobrir subdomínios

    // Define o cookie
    document.cookie = cookieName + "=" + encodeURIComponent(cookieValue) + 
                      "; " + expires + 
                      "; path=/; domain=" + domain + 
                      "; Secure; SameSite=None;";
})();
</script>


<script>
(function(){
    var cookieName = "Lead_ZipCode_GADs"; // Nome do cookie
    var cookieValue = "{{User Data | ZipCode (DLV) - order.address_info.postal_code}}" || ""; // Valor do cookie
    var expirationTime = 2592000 * 1000; // Um mês em milissegundos

    var date = new Date();
    date.setTime(date.getTime() + expirationTime);
    var expires = "expires=" + date.toUTCString();

    // Obtém o domínio de nível superior, removendo subdomínios
    var domainParts = location.hostname.split('.');
    var domain = domainParts.length > 2 
        ? "." + domainParts.slice(-2).join('.')  // Considera apenas os dois últimos segmentos
        : "." + location.hostname.replace(/^www\./i, ""); // Remove 'www.' para cobrir subdomínios

    // Define o cookie
    document.cookie = cookieName + "=" + encodeURIComponent(cookieValue) + 
                      "; " + expires + 
                      "; path=/; domain=" + domain + 
                      "; Secure; SameSite=None;";
})();
</script>


<script>
(function(){
    var cookieName = "Lead_Country"; // Nome do cookie
    var cookieValue = "{{User Data | Country (DLV) - order.address_info.country_code}}" || ""; // Valor do cookie
    var expirationTime = 2592000 * 1000; // Um mês em milissegundos

    var date = new Date();
    date.setTime(date.getTime() + expirationTime);
    var expires = "expires=" + date.toUTCString();

    // Obtém o domínio de nível superior, removendo subdomínios
    var domainParts = location.hostname.split('.');
    var domain = domainParts.length > 2 
        ? "." + domainParts.slice(-2).join('.')  // Considera apenas os dois últimos segmentos
        : "." + location.hostname.replace(/^www\./i, ""); // Remove 'www.' para cobrir subdomínios

    // Define o cookie
    document.cookie = cookieName + "=" + encodeURIComponent(cookieValue) + 
                      "; " + expires + 
                      "; path=/; domain=" + domain + 
                      "; Secure; SameSite=None;";
})();
</script>


<script>
(function(){
    var cookieName = "Lead_Hashed_FullStreet_GADs"; // Nome do cookie
    var cookieValue = "{{User Data | Street (DLV) - order.address_info. hashed_street_address}}" || ""; // Valor do cookie
    var expirationTime = 2592000 * 1000; // Um mês em milissegundos

    var date = new Date();
    date.setTime(date.getTime() + expirationTime);
    var expires = "expires=" + date.toUTCString();

    // Obtém o domínio de nível superior, removendo subdomínios
    var domainParts = location.hostname.split('.');
    var domain = domainParts.length > 2 
        ? "." + domainParts.slice(-2).join('.')  // Considera apenas os dois últimos segmentos
        : "." + location.hostname.replace(/^www\./i, ""); // Remove 'www.' para cobrir subdomínios

    // Define o cookie
    document.cookie = cookieName + "=" + encodeURIComponent(cookieValue) + 
                      "; " + expires + 
                      "; path=/; domain=" + domain + 
                      "; Secure; SameSite=None;";
})();
</script>
