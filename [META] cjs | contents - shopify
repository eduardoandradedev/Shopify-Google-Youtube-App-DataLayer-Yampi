function() {
  return {{dlv | ecommerce.items}}.map(function(product) { // Substitua pela variável que abre a matriz items
    return {
      id: product.item_id, // Verifique o formato do parâmetro id
      quantity: parseInt(product.quantity, 10), // Converte quantity para número inteiro
    };
  });
}
