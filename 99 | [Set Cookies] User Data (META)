<script>
(function(){
    var cookieName = "Lead_Email"; // Nome do cookie
    var cookieValue = "{{User Data | Email (DLV) - order.customer.email}}" || ""; // Valor do cookie
    var expirationTime = 2592000 * 1000; // Um mês em milissegundos

    var date = new Date();
    date.setTime(date.getTime() + expirationTime);
    var expires = "expires=" + date.toUTCString();

    // Obtém o domínio de nível superior, removendo subdomínios
    var domainParts = location.hostname.split('.');
    var domain = domainParts.length > 2 
        ? "." + domainParts.slice(-2).join('.')  // Considera apenas os dois últimos segmentos
        : "." + location.hostname.replace(/^www\./i, ""); // Remove 'www.' para cobrir subdomínios

    // Define o cookie
    document.cookie = cookieName + "=" + encodeURIComponent(cookieValue) + 
                      "; " + expires + 
                      "; path=/; domain=" + domain + 
                      "; Secure; SameSite=None;";
})();
</script>


<script>
(function(){
    var cookieName = "Lead_FirstName_Meta"; // Nome do cookie
    var cookieValue = "{{User Data | Norm - FirstName (META)}}" || ""; // Valor do cookie
    var expirationTime = 2592000 * 1000; // Um mês em milissegundos

    var date = new Date();
    date.setTime(date.getTime() + expirationTime);
    var expires = "expires=" + date.toUTCString();

    // Obtém o domínio de nível superior, removendo subdomínios
    var domainParts = location.hostname.split('.');
    var domain = domainParts.length > 2 
        ? "." + domainParts.slice(-2).join('.')  // Considera apenas os dois últimos segmentos
        : "." + location.hostname.replace(/^www\./i, ""); // Remove 'www.' para cobrir subdomínios

    // Define o cookie
    document.cookie = cookieName + "=" + encodeURIComponent(cookieValue) + 
                      "; " + expires + 
                      "; path=/; domain=" + domain + 
                      "; Secure; SameSite=None;";
})();
</script>


<script>
(function(){
    var cookieName = "Lead_LastName_Meta"; // Nome do cookie
    var cookieValue = "{{User Data | Norm - LastName (META)}}" || ""; // Valor do cookie
    var expirationTime = 2592000 * 1000; // Um mês em milissegundos

    var date = new Date();
    date.setTime(date.getTime() + expirationTime);
    var expires = "expires=" + date.toUTCString();

    // Obtém o domínio de nível superior, removendo subdomínios
    var domainParts = location.hostname.split('.');
    var domain = domainParts.length > 2 
        ? "." + domainParts.slice(-2).join('.')  // Considera apenas os dois últimos segmentos
        : "." + location.hostname.replace(/^www\./i, ""); // Remove 'www.' para cobrir subdomínios

    // Define o cookie
    document.cookie = cookieName + "=" + encodeURIComponent(cookieValue) + 
                      "; " + expires + 
                      "; path=/; domain=" + domain + 
                      "; Secure; SameSite=None;";
})();
</script>


<script>
(function(){
    var cookieName = "Lead_PhoneNumber_Meta"; // Nome do cookie
    var cookieValue = "{{User Data | PhoneNumber (DLV) - order.customer.phone_number}}" || ""; // Valor do cookie
    var expirationTime = 2592000 * 1000; // Um mês em milissegundos

    var date = new Date();
    date.setTime(date.getTime() + expirationTime);
    var expires = "expires=" + date.toUTCString();

    // Obtém o domínio de nível superior, removendo subdomínios
    var domainParts = location.hostname.split('.');
    var domain = domainParts.length > 2 
        ? "." + domainParts.slice(-2).join('.')  // Considera apenas os dois últimos segmentos
        : "." + location.hostname.replace(/^www\./i, ""); // Remove 'www.' para cobrir subdomínios

    // Define o cookie
    document.cookie = cookieName + "=" + encodeURIComponent(cookieValue) + 
                      "; " + expires + 
                      "; path=/; domain=" + domain + 
                      "; Secure; SameSite=None;";
})();
</script>


<script>
(function(){
    var cookieName = "Lead_City_Meta"; // Nome do cookie
    var cookieValue = "{{User Data | Norm - City (META)}}" || ""; // Valor do cookie
    var expirationTime = 2592000 * 1000; // Um mês em milissegundos

    var date = new Date();
    date.setTime(date.getTime() + expirationTime);
    var expires = "expires=" + date.toUTCString();

    // Obtém o domínio de nível superior, removendo subdomínios
    var domainParts = location.hostname.split('.');
    var domain = domainParts.length > 2 
        ? "." + domainParts.slice(-2).join('.')  // Considera apenas os dois últimos segmentos
        : "." + location.hostname.replace(/^www\./i, ""); // Remove 'www.' para cobrir subdomínios

    // Define o cookie
    document.cookie = cookieName + "=" + encodeURIComponent(cookieValue) + 
                      "; " + expires + 
                      "; path=/; domain=" + domain + 
                      "; Secure; SameSite=None;";
})();
</script>


<script>
(function(){
    var cookieName = "Lead_State_Meta"; // Nome do cookie
    var cookieValue = "{{User Data | State (DLV) - order.address_info.state}}" || ""; // Valor do cookie
    var expirationTime = 2592000 * 1000; // Um mês em milissegundos

    var date = new Date();
    date.setTime(date.getTime() + expirationTime);
    var expires = "expires=" + date.toUTCString();

    // Obtém o domínio de nível superior, removendo subdomínios
    var domainParts = location.hostname.split('.');
    var domain = domainParts.length > 2 
        ? "." + domainParts.slice(-2).join('.')  // Considera apenas os dois últimos segmentos
        : "." + location.hostname.replace(/^www\./i, ""); // Remove 'www.' para cobrir subdomínios

    // Define o cookie
    document.cookie = cookieName + "=" + encodeURIComponent(cookieValue) + 
                      "; " + expires + 
                      "; path=/; domain=" + domain + 
                      "; Secure; SameSite=None;";
})();
</script>


<script>
(function(){
    var cookieName = "Lead_ZipCode_Meta"; // Nome do cookie
    var cookieValue = "{{User Data | Norm - ZipCode (META)}}" || ""; // Valor do cookie
    var expirationTime = 2592000 * 1000; // Um mês em milissegundos

    var date = new Date();
    date.setTime(date.getTime() + expirationTime);
    var expires = "expires=" + date.toUTCString();

    // Obtém o domínio de nível superior, removendo subdomínios
    var domainParts = location.hostname.split('.');
    var domain = domainParts.length > 2 
        ? "." + domainParts.slice(-2).join('.')  // Considera apenas os dois últimos segmentos
        : "." + location.hostname.replace(/^www\./i, ""); // Remove 'www.' para cobrir subdomínios

    // Define o cookie
    document.cookie = cookieName + "=" + encodeURIComponent(cookieValue) + 
                      "; " + expires + 
                      "; path=/; domain=" + domain + 
                      "; Secure; SameSite=None;";
})();
</script>


<script>
(function(){
    var cookieName = "Lead_Country"; // Nome do cookie
    var cookieValue = "{{User Data | Country (DLV) - order.address_info.country_code}}" || ""; // Valor do cookie
    var expirationTime = 2592000 * 1000; // Um mês em milissegundos

    var date = new Date();
    date.setTime(date.getTime() + expirationTime);
    var expires = "expires=" + date.toUTCString();

    // Obtém o domínio de nível superior, removendo subdomínios
    var domainParts = location.hostname.split('.');
    var domain = domainParts.length > 2 
        ? "." + domainParts.slice(-2).join('.')  // Considera apenas os dois últimos segmentos
        : "." + location.hostname.replace(/^www\./i, ""); // Remove 'www.' para cobrir subdomínios

    // Define o cookie
    document.cookie = cookieName + "=" + encodeURIComponent(cookieValue) + 
                      "; " + expires + 
                      "; path=/; domain=" + domain + 
                      "; Secure; SameSite=None;";
})();
</script>


<script>
(function(){
    var cookieName = "Lead_UserID"; // Nome do cookie
    var cookieValue = "{{User Data | OrderID (DLV) - order.customer_id}}" || ""; // Valor do cookie
    var expirationTime = 2592000 * 1000; // Um mês em milissegundos

    var date = new Date();
    date.setTime(date.getTime() + expirationTime);
    var expires = "expires=" + date.toUTCString();

    // Obtém o domínio de nível superior, removendo subdomínios
    var domainParts = location.hostname.split('.');
    var domain = domainParts.length > 2 
        ? "." + domainParts.slice(-2).join('.')  // Considera apenas os dois últimos segmentos
        : "." + location.hostname.replace(/^www\./i, ""); // Remove 'www.' para cobrir subdomínios

    // Define o cookie
    document.cookie = cookieName + "=" + encodeURIComponent(cookieValue) + 
                      "; " + expires + 
                      "; path=/; domain=" + domain + 
                      "; Secure; SameSite=None;";
})();
</script>
